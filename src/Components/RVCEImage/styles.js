import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(
  () => ({
    layout: {
     // border: "2px solid red",
      width: "10%",
      padding: "5px",
    },
  }),
  { index: 1 }
);

export { useStyles };
